#include <stdio.h>
/**
 * main - entry point of program, prints the name of program
 * @argc: number of CL arguments
 * @argv: array of pointers to CL arguments
 * Return: 0 if successful
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
